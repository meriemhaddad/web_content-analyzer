# Environment configuration for Web Content Analysis Agent
# For security compliance, use system environment variables instead of .env files

# Server Configuration (Optional - defaults provided)
# HOST=127.0.0.1
# PORT=8000
# DEBUG=false

# Azure OpenAI Configuration
# Method 1: Set environment variables (Recommended for local development)
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
# AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o

# Method 2: Use Azure CLI authentication (Most Secure)
# Run: az login
# The application will automatically use your Azure CLI credentials

# Method 3: For Azure-hosted applications, use Managed Identity
# USE_MANAGED_IDENTITY=true

# MCP Fetch Server Configuration (OPTIONAL)
# If not provided, the service will use direct HTTP fetching
MCP_SERVER_URL=http://localhost:3000/mcp

# Content Analysis Settings
MAX_CONTENT_LENGTH=50000
ANALYSIS_TIMEOUT=30

# Batch Processing Settings  
MAX_BATCH_SIZE=10
BATCH_TIMEOUT=300

# Logging Configuration
LOG_LEVEL=INFO